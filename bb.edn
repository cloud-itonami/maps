{:tasks
 {test {:doc "Run the standalone maps suite"
        :task (shell "kbb -cp src:test -m maps.test-runner")}
  audit {:doc "Validate EDN and wire boundaries"
         :task (shell "kbb scripts/audit.cljk")}}}
